name: LeadFlow MLOps Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  train-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Model Training
      run: python train.py
      # In a real setup, this would connect to remote DVC storage and MLflow

    # In a real MLOps setup, you would add a step here to fail the build
    # if the model accuracy is below a certain threshold.

  build-and-deploy:
    needs: train-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
    
    - name: Simulate Deployment Trigger
      run: echo "🚀 Training passed. Triggering deployment to Render/Cloud..."
      # This is where you would run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}